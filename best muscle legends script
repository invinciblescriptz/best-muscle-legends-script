-- === WHITELIST SYSTEM (TEST MODE) ===
local player = game.Players.LocalPlayer

-- Change this to false if you want to test the kick
local isWhitelisted = true   -- ← Change to false to see "fuck you jester"

if not isWhitelisted then
    local screenGui = Instance.new("ScreenGui")
    local textLabel = Instance.new("TextLabel")
    
    textLabel.Size = UDim2.new(0.7, 0, 0.5, 0)
    textLabel.Position = UDim2.new(0.15, 0, 0.25, 0)
    textLabel.BackgroundTransparency = 0.3
    textLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    textLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    textLabel.TextScaled = true
    textLabel.Font = Enum.Font.GothamBlack
    textLabel.Text = "fuck you jester"
    textLabel.Parent = screenGui
    screenGui.Parent = player:WaitForChild("PlayerGui")
    
    wait(4)
    player:Kick("fuck you jester")
    return
end

print("✅ Whitelisted | Loading Invincible Fast Farming...")
